{
	// =========================
	// === LANGUAGE SETTINGS ===
	// =========================

	// RUST
	"[rust]": {
		"editor.defaultFormatter": "rust-lang.rust-analyzer"
	},

	// GO
	"[go]": {
		"editor.defaultFormatter": "golang.go"
	},

	// PYTHON
	"[python]": {
		"editor.codeActionsOnSave": {
			"source.fixAll": "explicit"
		},
		"editor.defaultFormatter": "charliermarsh.ruff"
	},
	// Venv activation is handled by mise via each repo's mise.local.toml.
	"python-envs.terminal.autoActivationType": "off",
	// Lint with the mise-managed ruff, not the binary bundled in the extension,
	// which updates on the extension's cadence and drifts from what the terminal
	// and CI run. The shim resolves per-directory, so a repo pinning ruff in its
	// mise.toml gets that version. Same reasoning as the taplo path below.
	"ruff.path": ["/home/user/.local/share/mise/shims/ruff"],

	// TYPESCRIPT
	"[typescript]": {
		"editor.codeActionsOnSave": {
			"source.addMissingImports.ts": "explicit",
			"source.fixAll.biome": "explicit",
			"source.organizeImports.biome": "explicit"
		},
		"editor.defaultFormatter": "biomejs.biome"
	},
	"biome.requireConfiguration": true,
	"biome.suggestInstallingGlobally": false,
	"eslint.workingDirectories": [{ "mode": "auto" }],
	"js/ts.updateImportsOnFileMove.enabled": "always",

	// JAVASCRIPT
	"[javascript]": {
		"editor.defaultFormatter": "biomejs.biome"
	},

	// SHELLSCRIPT
	"[shellscript]": {
		"editor.defaultFormatter": "foxundermoon.shell-format"
	},

	// MARKDOWN
	"[markdown]": {
		"editor.defaultFormatter": "esbenp.prettier-vscode"
	},
	"markdown-preview-enhanced.previewTheme": "github-dark.css",
	// The inline "markdownlint.config" object is deprecated in favour of a real
	// config file, so the rules live in ~/.markdownlint.jsonc and this only points
	// at them. It sets the base configuration: a .markdownlint.* file inside a
	// workspace still overrides it. WSL-only path, same as the taplo and ruff ones
	// above; a native Windows window has no config file and falls back to defaults.
	"markdownlint.configFile": "/home/user/.markdownlint.jsonc",

	// MAKEFILE
	"[makefile]": {
		"editor.defaultFormatter": "ms-vscode.makefile-tools"
	},
	"makefile.configureOnOpen": false,

	// DOCKERFILE
	"[dockerfile]": {
		"editor.defaultFormatter": "ms-azuretools.vscode-containers"
	},

	// HTML
	"[html]": {
		"editor.defaultFormatter": "esbenp.prettier-vscode"
	},

	// XML
	"[xml]": {
		"editor.defaultFormatter": "redhat.vscode-xml"
	},

	// CSS
	"[css]": {
		"editor.defaultFormatter": "esbenp.prettier-vscode"
	},

	// JSON / JSONC
	"[json][jsonc]": {
		"editor.codeActionsOnSave": {
			"source.fixAll.biome": "explicit"
		},
		"editor.defaultFormatter": "biomejs.biome"
	},

	// YAML
	"[yaml]": {
		"editor.defaultFormatter": "esbenp.prettier-vscode",
		"prettier.quoteProps": "consistent"
	},

	// TOML
	"[toml]": {
		"editor.defaultFormatter": "tamasfe.even-better-toml"
	},
	// Ensure detached WSL TOML files use the shared Taplo policy.
	"evenBetterToml.taplo.configFile.path": "/home/user/.config/mise/.taplo.toml",

	// GITHUB ACTIONS WORKFLOW
	"[github-actions-workflow]": {
		"editor.defaultFormatter": "github.vscode-github-actions"
	},

	// GROOVY (JENKINS)
	"[groovy]": {
		"editor.defaultFormatter": "NicolasVuillamy.vscode-groovy-lint"
	},
	"groovyLint.basic.loglevel": "error",

	// TERRAFORM
	"[terraform][terraform-vars]": {
		"editor.defaultFormatter": "hashicorp.terraform",
		"editor.formatOnSaveMode": "file"
	},

	// ASTRO
	"[astro]": {
		"editor.defaultFormatter": "astro-build.astro-vscode"
	},

	// SVELTE
	"[svelte]": {
		"editor.defaultFormatter": "esbenp.prettier-vscode"
	},
	"svelte.enable-ts-plugin": true,

	// ===================
	// === AI SETTINGS ===
	// ===================

	// GITHUB COPILOT
	"github.copilot.nextEditSuggestions.enabled": true,

	// ==========================
	// === EXTENSION SETTINGS ===
	// ==========================

	// AWS
	"aws.cloudformation.telemetry.enabled": false,
	"aws.telemetry": false,

	// GITHUB
	// Stops the "extension 'GitHub' wants to sign in" popup. Git Credential Manager
	// already handles login, so nothing is lost. Reopen terminals after changing.
	"git.terminalAuthentication": false,
	"github.gitAuthentication": false,

	// GITLENS
	"gitlens.codeLens.enabled": false,
	"gitlens.plusFeatures.enabled": false,
	"gitlens.statusBar.enabled": false,
	"gitlens.telemetry.enabled": false,

	// GRAFANA
	"grafana-vscode.telemetry": false,

	// PRETTIER
	// Format with the mise-managed prettier, not the copy bundled in the extension,
	// so Ctrl-S matches the terminal. Same reasoning as the ruff path above, but the
	// extension wants a module directory, not a bin, so this can't be a mise shim and
	// won't resolve per-directory. It also fully replaces workspace-local resolution:
	// a repo that pins its own prettier must re-point this in .vscode/settings.json
	// ("./node_modules/prettier"). Bump the version segment via the "latest" symlink,
	// which mise repoints on upgrade -- currently 3.9.6.
	"prettier.prettierPath": "/home/user/.local/share/mise/installs/prettier/latest/node_modules/prettier",

	// =======================
	// === EDITOR SETTINGS ===
	// =======================

	"editor.formatOnSave": true,
	"editor.minimap.enabled": false,
	"editor.tabSize": 2,
	"editor.unicodeHighlight.ambiguousCharacters": false,
	"explorer.compactFolders": false,
	"explorer.confirmDragAndDrop": false,
	"files.autoSave": "onFocusChange",
	"files.trimTrailingWhitespace": true,
	"git.mergeEditor": true,
	"git.openRepositoryInParentFolders": "never",
	"git.suggestSmartCommit": false,
	"remote.autoForwardPorts": false,
	"security.workspace.trust.untrustedFiles": "open",
	"telemetry.telemetryLevel": "off",

	// =========================
	// === TERMINAL SETTINGS ===
	// =========================

	"terminal.explorerKind": "integrated",
	// Use fish for integrated terminals on Linux/WSL only. These two stay adjacent
	// on purpose (out of alphabetical order): the profile below defines "fish".
	"terminal.integrated.defaultProfile.linux": "fish",
	"terminal.integrated.profiles.linux": {
		"fish": {
			"path": "/usr/bin/fish"
		}
	},
	"terminal.integrated.fontFamily": "Consolas Nerd Font",
	// Fixes TUI corruption in the integrated terminal on VS Code 1.129 (2026-07-30):
	// box borders missing, stray filled blocks, text drawn at the wrong origin.
	// Cause was xterm.js's WebGL glyph-atlas renderer -- NOT the Nerd Font and NOT
	// terminfo, so don't re-diagnose those. Proof it was paint-only: already-corrupted
	// scrollback repainted correctly the instant this flipped, without a new terminal.
	// Safe to retry "auto" after a VS Code update; revert here if corruption returns.
	"terminal.integrated.gpuAcceleration": "off",
	"terminal.integrated.initialHint": false,

	// =====================
	// === CHAT SETTINGS ===
	// =====================

	"chat.mcp.gallery.enabled": true,
	"chat.viewSessions.orientation": "stacked",

	// ===========================
	// === APPEARANCE SETTINGS ===
	// ===========================

	"vsicons.dontShowNewVersionMessage": true,
	"workbench.colorTheme": "GitHub Dark Default",
	"workbench.iconTheme": "vscode-icons",

	// ====================
	// === SCHEMA TRUST ===
	// ====================

	"json.schemaDownload.trustedDomains": {
		"https://biomejs.dev": true,
		"https://json-schema.org/": true,
		"https://json.schemastore.org/": true,
		"https://models.dev": true,
		"https://opencode.ai/": true,
		"https://raw.githubusercontent.com/": true,
		"https://schemastore.azurewebsites.net/": true,
		"https://www.schemastore.org/": true
	}
}
